language: node_js
node_js:
 — “8.11.3”
branches:
 only:
 — master
before_script:
 — npm install -g ionic cordova
script:
 — ionic cordova build browser — prod
deploy:
 provider: pages
 skip-cleanup: true
 github-token: c44e6414dd444c36bfd64ce6eb202b511a66a1cf
 keep-history: true
 on:
   branch: master
 local_dir: www